<script>
	import { Circle } from 'p45'
	import { grid, SVG } from './shared'

	//[doc:name] Circle
	//[doc:keywords] geometry, circle, dot, round
</script>

<SVG {...$$props} {grid}>
	<Circle radius="7" />
</SVG>
